version: '2'
services:
  mikutter:
    image: mikutter:mikutter-dev
    build:
      context: ./
      dockerfile: ./Dockerfile
    container_name: mikutter
    command: /bin/sh
    stdin_open: true
    tty: true

